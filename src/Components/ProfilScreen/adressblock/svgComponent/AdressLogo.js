import { Component } from "react";
import { StyleSheet, Text, View, TouchableOpacity } from 'react-native';
import { Svg, Path, Use } from 'react-native-svg';

export default class AdressLogo extends Component {
    render() {
        return (
            <View>
                    
            </View>
            );
    }
}